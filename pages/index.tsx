import type { NextPage } from "next";
import { Layout } from "../components/layouts";

const Home: NextPage = () => {
   return (
      <Layout>
         <h1>Cookie Page</h1>
      </Layout>
   );
};

export default Home;
